# This MVP does not require environment variables.
# Overpass API and OpenStreetMap tiles are accessed from the browser with public endpoints.
#
# If you later migrate reviews to Supabase, add values such as:
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
